mutation RegisterInRegistration($input: RegisterInput!) {
  registration {
    register(input: $input) {
      success
      accountId
      message
      requiresVerification
      appliedTenant
    }
  }
}
